window.addEventListener('load', (e)=>{
	let noSessionBtn = document.querySelector('.no-session-write-btn')
/*	noSessionBtn.addEventListener('click', (e)=>{
		let modalBox = new ModalBox({
			content:`로그인이 필요한 서비스 입니다.`,
			cancelBtnHide:true
		});

	})*/
});